{
  "name": "@uai/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'API build complete'",
    "test": "pytest",
    "lint": "ruff check . && black --check .",
    "clean": "rm -rf __pycache__ .pytest_cache *.egg-info"
  }
}
